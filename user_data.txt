#!/bin/bash
# Update packages
yum update -y

# Install Apache (httpd)
yum install httpd -y

# Start Apache immediately
systemctl start httpd

# Enable Apache to start on boot
systemctl enable httpd

# Create a test HTML page
echo "<h1>Test HTML Page on EC2</h1>" > /var/www/html/index.html